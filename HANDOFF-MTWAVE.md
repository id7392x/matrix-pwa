# Handoff — matrix-pwa · трек `mtwave`

> Для ИИ-агента, работающего от имени `mtwave`. Общее состояние проекта и правила — в `HANDOFF.md` (§1–3) и `AGENTS.md`/`COMMITS.md` (общие для всех).
> Трек ещё не подключён к работе — этот документ «лёгкого входа»: чтобы новый участник (никогда не пользовавшийся OpenCode) с нуля понял проект и смог начать.

## Статус трека

- **Не подключён.** Активной работы нет; вход — в любой момент: начать сессию со слов «я mtwave».
- Назначенные слайсы: колонка «Владелец» в `docs/04-ROADMAP.md` §3 (предложение — слайсы 5, 6; финально решает владелец репо).

## Экскурсия (агент проводит новому участнику в чате)

1. **Что это:** PWA Matrix-клиент на Svelte 5 (Runes) + TypeScript strict + Vite; хранение — Dexie/IndexedDB; сеть — Matrix JS SDK; E2EE — Vodozemac WASM (Слайс 4).
2. **Как запускать:** `pnpm install` → `pnpm dev` (`http://localhost:5173`). Проверки — `pnpm run check`, `pnpm test`, `pnpm run lint`.
3. **Как работать:** TDD (падающий тест → реализация → рефакторинг), `any` запрещён, IndexedDB только через Dexie, blast radius — только свой слайс.
4. **Как коммитить:** формат/scopes/авторство/подписи — `COMMITS.md`. Код-коммиты: автор `MTWave <его noreply>`, трейлер `Co-authored-by: OpenCode`. Доки трека (`MTWAVE`/`HANDOFF-MTWAVE`) — автор `OpenCode`, без трейлера.
5. **Как пушить:** ТОЛЬКО свои feature-ветки (workflow ветка→PR, `CONTRIBUTING.md`); `main` — никогда. После пуша — PR, дождаться approval `<repo-owner>`.
6. **Что дальше:** взять слайс с владельцем `mtwave` из `docs/04-ROADMAP.md`.

## Скиллы (краткое описание и использование)

| Скилл | Что делает | Когда использовать |
|---|---|---|
| `git-commit` | атомарный коммит по Conventional Commits (сообщение/сборка за агентом) | «закоммить» |
| `handoff` | фиксирует состояние сессии в этот файл | конец каждой сессии |
| `ponytail` | минимализм: stdlib/нативное/уже установленное раньше новых зависимостей | при выборе решения/зависимости |
| `boy-scout-rule` | тронутый код оставить чище | правка существующих файлов |
| `code-review` | ревью по стандартам + соответствие слайсу | перед коммитом/PR по слайсу |
| `diagnosing-bugs` | диагностика падений/регрессий | «почему падает / не работает» |

## Следующий шаг

- Пока не назначен. Когда владелец репо выделит слайс — начать с TDD-тестов из раздела `docs/04-ROADMAP.md` соответствующего слайса.

## Хвосты трека

- (пусто)
